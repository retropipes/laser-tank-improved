function love.conf(t)
    t.identity = "com.puttysoftware.lasertankimproved"
    t.version = "11.3"
    t.window.title = "LaserTank"
    -- Disable modules the game does not use
    t.modules.video = false
end